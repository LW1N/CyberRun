using UnityEngine;

public class SetBackgroundColor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Set the camera's background color to gray
        Camera.main.backgroundColor = Color.gray;
    }
}
